# Rest Timer Sound

Place `rest-done.mp3` here for a custom bell/chime when the rest timer finishes **in foreground**.

## iOS Expo Go (background)

In Expo Go, when the app is in background, the notification uses the **system default sound**, not this custom mp3. See `src/components/RestTimer.README.md` for details.
